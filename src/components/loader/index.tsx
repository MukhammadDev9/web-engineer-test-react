import { FC } from "react"

const Loader: FC = () => {
    return (
        <div className="loader">
            <div className="loader__spin">Loading...</div>
        </div>
    )
}

export default Loader
